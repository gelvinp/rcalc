# Renderer commands
- Options (screaming)

# Vector operators
- dot
- cross
- len
- lensqr
- add, (scalar)mul, (scalar)div, sub, neg, min, max, avg

# Matrix operators
- add, sub, (scalar) mul, (scalar)div, tpose, (vec)mul, (mat)mul
- det (determinate)
- transmat (Vec2 -> mat2 translation matrix, vec3 vec4 same)
- scalemat (Vec2 -> mat2 scale matrix, vec3 vec4 same)
- rotmat (scalar -> mat2 rotation matrix, vec3 -> mat3 rotation matrix)

# Technical Debt
- Find a BigInt
- Vec2/3/4, Mat2/3/4
- Options, allow changing font scale, light mode / dark mode / read from system?
- Arrow keys up and down to scroll scratchpad history
- Tab autocomplete commands and operators (that are valid based on param types)
- Refactor Renderer into ImGuiRenderer enabled at build time, allow runtime renderer selection
- TUI Renderer
- Use a perfect hash generator for operator and command maps
