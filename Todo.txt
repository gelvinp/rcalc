# Track 0
 - Matrix datatype
 - Matrix operators
    - add, sub, (scalar) mul, (scalar)div, tpose, (vec)mul, (mat)mul
    - det (determinate)
    - transmat (Vec2 -> mat2 translation matrix, vec3 vec4 same)
    - scalemat (Vec2 -> mat2 scale matrix, vec3 vec4 same)
    - rotmat (scalar -> mat2 rotation matrix, vec3 -> mat3 rotation matrix)
    - Construct from vectors
    - Column and row accessors

# Track 1
 - ~~Refactor renderer~~
 - ~~Modules "enable" system (platform enables glfw, stb_image; renderer enables imgui, backends)~~
 - ~~Modules "static" system (when module is enabled, potentially offer a way to statically link)~~
 - Add sqlite module (enabled by platforms, have a built in preferences system) (maybe?)
 - Create options controller, platform integration with data directory, save/load interface
 - Create lib target
 - Create TUI renderer

# Track 2
 - ~~Refactor OperatorMap generator~~
    - ~~Get rid of string comparisons for type dispatch~~
    - ~~Get rid of duplication in implementation generation~~
    - ~~Add consistent ordering for type sets, and expose an alphabetical list of operators, in alphabetical by category~~
 - Add use_gperf=/usr/bin/gperf option
 - ~~Add Examples doctag~~
 - Add "Category" doctag (No category is an option, goes at the top of the list)
 - Refactor CommandMap generator
    - Scons flags to enable/disable scopes
    - Inherit improvements from OperatorMap
    - Expose all the registered scopes 
 - Improve help page for imgui renderer